document.addEventListener('DOMContentLoaded',()=>{const inputs=[...document.querySelectorAll('.tech-option input[data-price]')];if(!inputs.length)return;const base=100;const format=v=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL',minimumFractionDigits:0});const update=()=>{const selected=inputs.filter(i=>i.checked);const total=base+selected.reduce((sum,i)=>sum+Number(i.dataset.price),0);document.querySelectorAll('#total-price,#total-price-box').forEach(el=>el.textContent=format(total));const count=document.querySelector('#selection-count');if(count)count.textContent=`${selected.length} ${selected.length===1?'tecnologia selecionada':'tecnologias selecionadas'}`};inputs.forEach(input=>input.addEventListener('change',update));update()});